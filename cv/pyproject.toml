[project]
name = "vanguard-cv"
version = "0.1.0"
description = "RF-DETR wrapper, TensorRT compilation, Triton fused-preprocessing kernel"
requires-python = ">=3.12"

dependencies = [
    "torch",
    "torchvision",
    "triton",
    "tensorrt",
]

