[project]
name = "vanguard-memory"
version = "0.1.0"
description = "PostgreSQL/TimescaleDB/pgvector schema, migrations, hybrid RAG queries"
requires-python = ">=3.12"
dependencies = [
    "asyncpg",
    "pgvector"
]
